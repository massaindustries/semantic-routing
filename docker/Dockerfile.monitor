FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY monitor/ ./monitor/
COPY core.py .

EXPOSE 8001

CMD ["python", "-m", "uvicorn", "monitor.dashboard:app", "--host", "0.0.0.0", "--port", "8001"]
